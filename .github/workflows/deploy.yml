name: Deploy

on:
  push:
    branches:
      - master # Change this to the branch you want to trigger the deployment from

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up SSH connection
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      # - name: Deploy to server
      #   run: |
      #     ssh -o StrictHostKeyChecking=connect@artoa.io@${{ secrets.HOST }} 'bash -s' << 'EOF'
      #       cd /root/ArtOaa
      #       git pull
      #     EOF

           - name: Deploy to server
        run: |
          ssh -o StrictHostKeyChecking=connect@artoa.io <(echo "cd /root/ArtOaa; git pull") ${{ artoa.io }}
